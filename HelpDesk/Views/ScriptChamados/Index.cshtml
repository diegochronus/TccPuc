@model IEnumerable<HelpDesk.Models.ScriptChamado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DesScript)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdSubGategoria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DesScript)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSubGategoria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdScriptChamado">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdScriptChamado">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdScriptChamado">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
