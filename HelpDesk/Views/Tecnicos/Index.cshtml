@model IEnumerable<HelpDesk.Models.Tecnico>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdSetor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdNivelOperacional)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSetor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdNivelOperacional)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTecnico">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTecnico">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTecnico">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
